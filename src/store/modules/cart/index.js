import actions from "./actions";
import mutations from "./mutations";
const state = {
  addedProducts: [
    {
      name: "oooooo",
      icon: "222222222",
    },
  ],
};

const getters = {};

export default {
  state,
  mutations,
  actions,
  getters,
};
